services:
  - type: web
    name: keiba-api
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        sync: false  # Render Dashboard で設定
      - key: NODE_ENV
        value: production
